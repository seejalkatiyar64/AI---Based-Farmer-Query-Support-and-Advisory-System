# 🌾 KrishiMitra AI – Predictive Farming Intelligence System
### Smart India Hackathon (SIH) Final Round Project

---

## 🎯 Project Overview

KrishiMitra AI is a full-stack AI-powered farming platform designed for Indian farmers. It provides:
- 🌡️ **Predictive weather alerts** before problems occur
- 🤖 **Multilingual AI chatbot** (Hindi + English) powered by Claude/OpenAI
- 📸 **Crop disease detection** via image upload
- 💰 **Profit calculator** with crop comparison table
- 🎤 **Voice input** (Web Speech API – no internet needed for voice)
- 📞 **IVR concept** for feature-phone farmers

---

## 📁 Folder Structure

```
KrishiMitra/
├── frontend/                    # React + Tailwind UI
│   ├── src/
│   │   ├── App.jsx             # Main app with all 4 pages
│   │   ├── main.jsx            # React entry point
│   │   └── index.css           # Tailwind + custom styles
│   ├── index.html              # HTML shell
│   ├── package.json
│   ├── vite.config.js          # Vite + API proxy config
│   └── tailwind.config.js
│
├── backend/                     # Node.js + Express API
│   ├── server.js               # All API endpoints
│   ├── package.json
│   └── db.json                 # Auto-created JSON database
│
└── README.md
```

---

## 🚀 Setup & Run Instructions

### Prerequisites
- Node.js v18+ 
- npm v9+

### Step 1: Start Backend
```bash
cd backend
npm install
node server.js
# Server runs at http://localhost:5000
```

### Step 2: Start Frontend
```bash
cd frontend
npm install
npm run dev
# App runs at http://localhost:3000
```

### Step 3: Open in Browser
Visit `http://localhost:3000`

---

## 🔑 API Keys (Optional – App works without them using mocks)

For real AI responses, add to `frontend/.env`:
```
VITE_ANTHROPIC_API_KEY=sk-ant-...
```

For real weather, add to `backend/.env`:
```
WEATHER_API_KEY=your_openweathermap_key
```

**Note:** The prototype works fully with mock data. No API keys required for demo.

---

## 🧠 How Each Feature Works

### 1. Dashboard
- Shows farmer profile (stored in React state, editable)
- Real-time weather card with 5-day forecast (mock data)
- Smart alerts generated by rule-based prediction engine
- Crop health progress bars
- Rotating marquee alerts

### 2. AI Chatbot
- Powered by Claude Sonnet API (called directly from browser)
- System prompt includes farmer profile + current weather
- Responds in Hindi by default
- Voice input via Web Speech API (`webkitSpeechRecognition`)
- Conversation history maintained in component state

### 3. Disease Detection
- Drag-and-drop or tap-to-upload image
- Frontend preview with FileReader API
- Calls `/upload` backend endpoint
- Backend runs mock classifier (replace with Plant.id or TF Serving)
- Returns disease name, confidence %, treatment, prevention steps

### 4. Profit Calculator
- Select two crops, adjust area with slider
- Calculates cost, revenue, profit, ROI per hectare × area
- Comparison table with winner highlighted
- "AI Analysis" button calls Claude for personalized recommendation
- Government schemes listed at bottom

### 5. Rule-Based Prediction Engine (Backend)
```
IF humidity > 80% AND temp > 25°C → fungal risk HIGH
IF temp > 30°C AND humidity < 40% → aphid pest risk HIGH  
IF rain > 50% → irrigation alert
IF temp < 5°C → frost risk CRITICAL
```

### 6. IVR / Missed Call System (Concept)
- Farmer dials a number → call drops (missed call)
- System receives webhook → looks up farmer profile
- Generates Hindi voice script
- Calls back using Twilio/Exotel with personalized alert
- `POST /ivr/missed-call` endpoint simulates this flow

### 7. Offline Mode (Concept)
- Service Worker caches app shell + static assets
- Last weather data stored in localStorage
- AI suggestions pre-generated and cached
- Sync when connectivity restored

---

## 🌐 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/health` | Health check |
| POST | `/chat` | AI chatbot response |
| POST | `/predict` | Pest/disease risk prediction |
| POST | `/upload` | Image upload + disease detection |
| GET | `/weather?city=Pune` | Weather data |
| POST | `/profit` | Crop profit calculation |
| GET | `/farmer/:id` | Get farmer profile |
| POST | `/farmer` | Create farmer profile |
| PUT | `/farmer/:id` | Update farmer profile |
| POST | `/ivr/missed-call` | IVR simulation |

---

## 📊 Sample API Calls

### Predict Disease Risk
```bash
curl -X POST http://localhost:5000/predict \
  -H "Content-Type: application/json" \
  -d '{"humidity": 85, "temp": 28, "rain": 60, "crop": "wheat"}'
```

### Calculate Profit
```bash
curl -X POST http://localhost:5000/profit \
  -H "Content-Type: application/json" \
  -d '{"crop": "cotton", "area": 3}'
```

### Upload Image
```bash
curl -X POST http://localhost:5000/upload \
  -F "image=@/path/to/leaf.jpg"
```

---

## 🏆 SIH Winning Features

1. **Farmer-First UX** – Large buttons, Hindi labels, voice input
2. **Predictive (not reactive)** – Warns before problems occur
3. **Offline-aware design** – Works in low-connectivity rural areas
4. **IVR integration** – Reaches feature-phone farmers (no smartphone needed)
5. **Personalized AI** – Tailored to each farmer's crop, location, history
6. **Multi-modal input** – Text, voice, and image
7. **Government scheme integration** – PM Kisan, KCC, crop insurance
8. **Bilingual** – Hindi + English support throughout

---

## 🛠️ Production Upgrades

| Feature | Current | Production |
|---------|---------|-----------|
| AI Chat | Claude API (browser) | Backend proxy with rate limiting |
| Disease Detection | Mock classifier | Plant.id API / TF Serving model |
| Weather | Mock data | OpenWeatherMap / IMD API |
| Database | JSON file | MongoDB Atlas |
| Auth | None | OTP-based (phone number) |
| IVR | Simulation | Twilio / Exotel |
| Offline | Concept | Service Worker + PWA |

---

## 👥 Team KrishiMitra
Smart India Hackathon 2024 – Agriculture & Allied Sectors

*Built with ❤️ for India's 140 million farmers*
